# STAY: Iron Vanguard — 설계 문서

오리지널 IP의 방치형 웹게임 (상업 배포 목표: CrazyGames 1차, 자체 배포 병행).
v1은 니케 팬 프로젝트로 시작했으나 v2에서 오리지널 IP로 리브랜딩 — 타사 IP 명칭·연상 요소 사용 금지.

## 세계관 (리브랜딩)
- 타이틀: **STAY: Iron Vanguard** (로고 "STAY // idle")
- 기계 재해 **보이드(Void)** 에 맞서는 인조 전사 **아스트라(Astra)** 부대를 지휘
- 진영 4개: 오리온 / 헬릭스 / 테라 / 노마드
- 시그니처 시스템 명칭: 버스트 → **오버드라이브** (I·II·III 체인 → 풀 오버드라이브). UI 문자열만 변경, 코드 식별자(burst 등)는 유지

## 기술 스택
- 바닐라 HTML/CSS/JS, 외부 의존성 0, 빌드 없음 (index.html 더블클릭으로 실행 가능)
- 저장: localStorage (버전 필드 포함, 5초 주기 자동저장 + beforeunload 저장)
- 파일 구조 (각 파일 ~200줄 이내, 단일 책임):
  - `index.html` — 레이아웃, 탭 구조
  - `css/style.css` — 다크 SF 테마 (네온 시안/마젠타 포인트)
  - `js/data.js` — 니케 로스터, 상수, 밸런스 테이블
  - `js/state.js` — 게임 상태, 저장/로드, 리셋
  - `js/combat.js` — 자동 전투 루프, 스테이지, 버스트
  - `js/gacha.js` — 모집(가차)
  - `js/idle.js` — 오프라인 정산, 전초기지
  - `js/ui.js` — 렌더링, 탭, 숫자 포맷(K/M/B/T…)
  - `js/main.js` — 초기화, 게임 루프(250ms 틱)

## 핵심 시스템

### 1. 니케 (캐릭터)
- 등급: R / SR / SSR. 버스트 타입: I / II / III. 무기: AR/SMG/SR/RL/MG/SG (플레이버). 기업: 엘리시온/미실리스/테트라/필그림
- 스탯 공식: `atk = baseAtk × (1 + 0.1 × (level − 1)) × (1 + 0.3 × core)`
  - level: 크레딧으로 업그레이드, 비용 = `50 × 1.12^(level−1)`
  - core(한계돌파): 중복 획득 시 +1, 최대 3
- baseAtk 기준: R=10, SR=18, SSR=32 (개체별 ±20% 변주)
- 로스터 20명 (전원 오리지널 명명, id도 함께 교체). 시작 지급: 유나(SR, III), 미르(SR, II), 하나(R, I)
- 확정 로스터 (기존 개체의 rarity/burst/weapon/corp/baseAtk 수치는 유지하고 이름·id·진영만 교체):
  - SSR: 이그니스(ignis), 카르민(carmine), 노바(nova), 아리아(aria), 루멘(lumen), 세레스(ceres), 운디네(undine), 벨라(vera), 프리즘(prism)
  - SR: 유나(yuna), 미르(mir), 리브(liv), 텐(ten), 볼트(volt), 새라(sera)
  - R: 하나(hana), 피코(pico), 로카(roka), 츠키(tsuki), 제로(zero)
  - 매핑(수치 승계): 홍련→이그니스, 스칼렛→카르민, 모더니아→노바, 리터→아리아, 라플라스→루멘, 도로시→세레스, 노아→운디네, 엑시아→벨라, 앨리스→프리즘, 라피→유나, 아니스→미르, 유니→리브, 미란다→텐, 벨로타→볼트, 솔린→새라, 네온→하나, 소다→피코, 마리안→로카, 미카→츠키, 엔비→제로
  - 진영 교체: 엘리시온→오리온, 미실리스→헬릭스, 테트라→테라, 필그림→노마드

### 2. 자동 전투
- 스테이지: 챕터-스테이지 표기 (1-1, 1-2 … 챕터당 10스테이지, 무한 생성)
- 적 HP = `100 × 1.18^stageIndex`. 5의 배수 스테이지는 보스: HP ×5, 보상 ×5, 30초 시간제한
- 250ms 틱마다 편성 5인 총 DPS만큼 적 HP 감소. 처치 시 크레딧/젬 보상 후 자동 진행
- 보스 시간제한 실패 시 직전 스테이지 반복 파밍 모드 (수동 재도전 버튼)

### 3. 버스트 시스템 (시그니처 메커니즘)
- 전투 중 게이지 충전 (처치·시간 기반). 만충 시 풀버스트 발동
- 조건: 편성에 버스트 I, II, III 타입이 모두 1명 이상 → 전략적 편성 유도
- 풀버스트: 10초간 데미지 ×2.5, 이후 쿨다운. UI에 I→II→III 순차 연출(텍스트/CSS)

### 4. 모집 (가차)
- 일반 모집: 젬 300/회, 10연 2,700
- 확률: SSR 4% / SR 43% / R 53%. 중복 획득 → 해당 니케 코어 +1 (만돌파 시 크레딧 환급)

### 5. 방치 보상
- 오프라인 정산: 이탈 시간 × 현재 스테이지 기준 크레딧/젬 수익률 (상한 12시간). 복귀 시 정산 모달
- 전초기지: 온라인 중 초당 크레딧 자동 수급. 크레딧으로 시설 레벨업 → 수급량 증가

### 6. UI 탭
1. **전투** — 스테이지, 적 HP바, 버스트 게이지, 총 DPS, 편성 슬롯 5개
2. **니케** — 로스터 목록(등급·버스트·레벨·코어), 레벨업 버튼, 편성 토글
3. **모집** — 1회/10연 버튼, 결과 연출, 확률 표기
4. **전초기지** — 시설 레벨업, 수급량 표시, 설정(저장 리셋 — 2단 확인)

## 캐릭터 아이콘 (v2.1) — 프로시저럴 SVG
외부 이미지 에셋 0 원칙 유지. `js/icons.js`의 `nikkeIcon(id)` 함수가 인라인 SVG 문자열을 생성한다.
- 구성: 등급색 테두리 프레임(R 회색 / SR 보라 / SSR 골드 — 기존 rarity 색상 변수 재사용) + 진영색 배경 그라데이션 + 이름 첫 글자 + 무기 글리프
- 진영 색: 오리온=시안, 헬릭스=마젠타, 테라=그린, 노마드=앰버. id 해시로 그라데이션 각도/명도 미세 변주 (동일 진영끼리도 구분)
- 무기 글리프 6종(AR/SMG/SR/RL/MG/SG): 단순 SVG 도형으로 직접 드로잉 (외부 폰트·이모지 금지)
- 적용처: 로스터 카드, 편성 슬롯, 모집 결과 카드. 모집에서 SSR은 골드 글로우 연출(CSS 애니메이션)
- 결정적(deterministic): 같은 id는 항상 같은 아이콘. 새 캐릭터 추가 시 자동 생성

## 수익화 (v2)

### 광고 어댑터 계층 — `js/ads.js`
모든 광고 호출은 어댑터 인터페이스를 통해서만 수행. 게임 로직에 SDK 직접 호출 금지.
- 인터페이스: `init()`, `showRewarded(onReward, onFail)`, `showBanner()`, `hideBanner()`, `gameplayStart()`, `gameplayStop()`
- 어댑터 선택: URL 파라미터 `?ads=mock|crazygames|none` (기본 mock). 호스트가 `crazygames.com` 포함 시 자동으로 crazygames
- **mock 어댑터**: 개발/데모용 — 3초 카운트다운 오버레이 후 보상 지급 (실제 광고 흐름 시뮬레이션)
- **crazygames 어댑터**: 공식 HTML5 SDK v3 사용. 구현 전 공식 문서(https://docs.crazygames.com/) 로 API를 반드시 검증할 것.
  보상형 광고 요청, gameplayStart/Stop 이벤트, 로딩 이벤트 연동. 배너는 포털이 자체 운영하므로 게임 내 배너 비활성
- **none**: 모든 광고 비표시 (배너 슬롯 숨김)
- SDK 스크립트는 해당 어댑터 활성 시에만 동적 로드 (mock/none일 때 외부 요청 0 유지)

### 보상형 광고 배치 3종
1. **오프라인 정산 ×2** — 복귀 모달에 "광고 보고 2배 수령" 버튼. 시청 완료 시 정산 재화 1배 추가 지급
2. **무료 젬** — 전초기지 탭에 "광고 보고 젬 {AD_GEM_REWARD} 획득" 버튼. 쿨다운 15분 (`state.adGemReadyAt` 영속)
3. **DPS 부스트** — 전투 탭에 "광고 보고 30분 DPS ×2" 버튼. `state.boostUntil` (epoch ms) 영속, 오버드라이브 배율과 곱연산. 활성 중 남은 시간 표시, 중복 시청 시 시간 연장
- 밸런스 상수: `AD_GEM_REWARD: 150`, `AD_GEM_COOLDOWN_MS: 15분`, `AD_BOOST_MULT: 2`, `AD_BOOST_DURATION_MS: 30분`
- 오프라인 정산에는 부스트 미적용 (악용 방지)

### 배너
- 하단 고정 슬롯 `#banner-slot` (320×50 중앙정렬, 자리 예약으로 레이아웃 시프트 방지)
- mock: 플레이스홀더 표시. crazygames: 숨김. 자체 배포 시 AdSense 코드를 붙일 수 있도록 슬롯만 마련 (v2에서는 실 코드 미삽입)

### CrazyGames 광고 정책 준수 (v2.3 — 제출 전 필수)
공식 문서(docs.crazygames.com/requirements/ads/) 기준:
- **광고 중 게임 일시정지 (리젝 사유)**: 모든 광고(mock 포함) adStarted 시 게임 루프 정지 + `gameplayStop()`, adFinished/adError 시 재개 + `gameplayStart()`. main.js에 `pauseLoop()`/`resumeLoop()` 제공, ads.js가 호출
- **미드게임 광고**: `requestAd('midgame')`. 자연 전환점 2곳 — ① 보스 재도전 버튼 클릭 직후 ② 복귀 정산 모달 수령 클릭 직후. 빈도는 SDK가 자동 관리(3분 캡, 조기 요청은 무시됨)라 게임에서 별도 쿨다운 불요. mock은 짧은 전면 오버레이로 시뮬레이션, none은 즉시 통과
- **보상형 규칙**: adFinished에서만 보상(adError 무보상), 광고 시청임을 문구로 명시("광고 보고 ~"), 하나의 보상에 광고 1회만
- **배너**: SDK v3에 배너 API가 있으나 "게임플레이 화면 노출 금지" 규칙상 단일 화면 방치형인 본 게임은 CrazyGames 모드에서 배너 미사용 유지 (자체 배포 모드 전용)
- **실패 내성**: adblock/unfilled/adsDisabledBasicLaunch 어느 경우에도 게임 정상 동작, 애드블록 사용자 불이익 금지

### 배포 전략
- 1차: CrazyGames 제출 (보상형 수익쉐어). 2차: Poki 도전. 병행: GitHub/Cloudflare Pages 자체 배포 + 추후 AdSense/H5 Games Ads
- 저장 키: 리브랜딩과 함께 `stay_iron_vanguard` 로 교체, SAVE_VERSION=2 (구버전 세이브는 새 게임 처리 — 프리릴리즈라 마이그레이션 불요)

## 완료 기준
- `node --check` 전 JS 파일 통과
- 브라우저에서 열면 즉시 플레이 가능: 전투 진행 → 재화 획득 → 모집 → 편성 → 레벨업 → 풀버스트 → 오프라인 정산 전 루프 동작
- 새로고침 후 진행 상황 유지
